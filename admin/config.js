window.CMS_CONFIGURATION = {
  CMS_MANUAL_INIT: true,
  config: {
    backend: {
      name: "github",
      repo: "albo3/tw5",
      branch: "main",
      base_url: "https://api.github.com",
      auth_endpoint: "auth",
    },
    // ... rest of your config
  },
};
